# 显卡供电开关与硬件管理

## 1. 硬件背景与策略
* **独立显卡**：NVIDIA GeForce GTX 950M。
* **默认功耗策略**：开机自动切断 PCI 供电（由 `gpu-poweroff.service` 管理，平时维持 0W 功耗，发热与能耗最低）。

## 2. 常用操作命令
* **上电开启显卡**：`sudo gpu-on`（需要跑本地模型、硬件加速或 CUDA 时使用）；
* **断电关闭显卡**：`sudo gpu-off`（任务完成后执行断电复原）。

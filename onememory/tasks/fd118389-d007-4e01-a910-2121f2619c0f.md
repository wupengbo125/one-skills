# 案卷 [fd118389-d007-4e01-a910-2121f2619c0f]

## 背景诉求
- one-todo 赛马后要分高下：开判官 agent 对比各 worktree 产出结论。
- 单马/本地直跑也要能审；审是一个功能（单马审一份，多马逐匹审+挑胜者）。
- 判官提示词必须带任务原文（否则可能选错：代码漂亮但不对需求）。
- 仲裁结论四段式（逐匹审/胜者/综合优点/修改建议），结论一键发回干活会话继续改，改完重审成环。
- 另有独立任务 #31：全军出击长按 2 秒改 1 秒。

## 关键决策
- 仲裁与待办状态隔离：turn_ended 先翻仲裁，workspace.archived 清判官分支。
- 候选目录以 git worktree 按分支为权威；projectRootPath 永不作候选。
- 结论文件 `~/.paseo/plugin-data/one-todo/arbitrations/<todoId>.md`，仲裁首行「胜者: N」，审核首行「结论: 通过/不通过」，缺失判失败。
- agy 终端判官无 turn 事件：靠结论轮询（4s）+ 终端存活检查翻转，连续 3 次查不到判失败。
- 发回会话：agent 用 `agents.ref().send()`，终端用 `write()+sendKeys(Enter)`；审核发那一匹，仲裁解析胜者编号发对应马。

## 涉及产物
- 新增 `paseo-plugins/one-todo/server/arbitration.ts`；改 shared/todo.ts（arbitration kind/terminalId/RPC），server/{executor,todo}.ts，index.server.ts，client/{todo.tsx,model.ts}，README。
- 两轮独立 sub-agent 等号审查均通过；`npm run typecheck` 通过。

# 01a0b98c-713f-72b3-8f42-cb72b55e4460 会话ID获取机制与生活规范同步

## 背景诉求
规范会话 ID 获取逻辑，杜绝 Agent 伪造或跳过 ID；同时提交 one-life 写入后 pull 本地仓库并汇报规范。

## 关键决策与排查
- 会话 ID 获取确立三级梯次：自知（上下文注入）→ 探针（$CODEBUDDY_SESSION_ID 等）→ 兜底阻断（硬中断向用户索取，严禁伪造）。
- 排查回显误判：Harness 过滤层对工具流中符号过滤，核实文件底层字节确认箭头完整。
- one-life 规范明确远端写入完成后需拉取本地仓库同步并汇报。

## 涉及改动
- `one-memory/references/write-memory.md`：三级 Session ID 策略。
- `one-life/references/note.md`：远端操作后本地 git pull 规范。

# one-todo 支持仅标题开跑并合并提示词

## 背景与诉求
用户有时候建立任务只有标题不想重复写内容。如果不发标题给 Agent，又逼着用户把内容重新打一遍。

## 改动内容
1. `paseo-plugins/one-todo/client/todo.tsx`：开跑移除对内容的强制校验，允许仅填标题直接开跑。
2. `paseo-plugins/one-todo/server/executor.ts`：提示词非空校验放宽为标题或内容至少一项；提示词合并拼接标题与内容（有内容时拼为 `标题\n\n内容`，无内容时直接发送标题）。

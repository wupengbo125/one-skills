# 2026-09-20 22:30 会话案卷（会话 01a0bf03）

## 海马仓流水原始摘要（超长，已缩为一句，详情保留于此）

排查 Tailscale 慢：根因是 Windows↔本机 tailscale 直连走的是跨运营商公网 IPv6（设备移动 2409 ↔ 本机电信 240e），丢包 0~33% 随机波动（同一时刻两台设备一台 0% 一台 20%），SSH 对丢包零容忍故卡到动不了，而 frp 走 IPv4（192.140.188.104）稳定 0% 丢包故一直快；排除 exit node、Windows 代理（非 TUN、退出进程仍卡）、MTU；确认 Tailscale 无关闭 IPv6 的开关（官方仅 feature request #13705），要改 IPv4 只能禁用网卡 IPv6。过程中顺带揪出并修复：本机 mihomo TUN auto-route 把 frpc 出站流量吞进 TUN（连接本地地址变 198.18.0.1、delivery_rate 塌到 1.6Mbps、经 frp 的 SSH 卡死），加 tun.route-exclude-address 排除 192.140.188.104/32 并重启 mihomo+frpc 后连接 mss 8948 回到 1398、经 frp 的 SSH 0.53s 恢复；卷轴《自建Tailscale自定义DERP中继经公共FRP暴露》补坑 11（mihomo TUN 吞 frpc 出站流量的判定与修法）、排障速查加一行、坑 5 补注、修正过时的 frpc 配置路径。用户最终决定先不折腾 IPv6。

# [01a0af6f-8886-7750-9458-6335a77bc184] 重构 one-memory 钩子脚本为 install-hook.sh

会话 ID：01a0af6f-8886-7750-9458-6335a77bc184
时间：2026-09-17 21:07
状态：已完成

## 背景与诉求
清理 `one-memory/hooks/` 目录历史包袱：
1. 原有 `install.sh` 与 `install-hook.sh` 命名割裂，且与其他技能规范（`one-wiki/hooks/install-hook.sh`、`one-life/hooks/install-hook.sh`）不统一。
2. 历史脚本中每次安装都尝试删除早已不存在的 `commit-msg` 钩子。
3. 统一规范为单个 `install-hook.sh`，批量向 `~/onespace/github/*` 安装 `pre-commit` 门禁，向 `one-hippocampus` 安装 `post-commit` 自动索引同步。

## 涉及文件
- `one-memory/hooks/install-hook.sh`：重写为批量分发 pre-commit 及定向安装 post-commit
- `one-memory/hooks/install.sh`：删除冗余脚本
- `one-memory/README.md`：同步目录树与安装文档
- `onememory/timeline.md`：记录工作流水

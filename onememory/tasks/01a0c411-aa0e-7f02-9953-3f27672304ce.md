# 01a0c411-aa0e-7f02-9953-3f27672304ce

## 删除 one-memory 的 pi-extension 扩展层

背景诉求：用户用 one-writing-skill 让我审 one-memory，指出多个问题后决定不再要 Pi/OMP 扩展层，直接删掉。

改动：
- 删 `one-memory/pi-extension/`（index.ts、memory-rules.md）
- 删 `one-memory/package.json`（纯 pi 清单，指向已删目录）
- `one-memory/README.md`：清目录树 pi 条目、删"模式 B：Pi & OMP 原生扩展"整段、删"收工记忆"小节、"三轨"改"两轨"
- `CODE_WIKI.md`：删目录树 pi 条目、删 one-memory 扩展接口行、路由表去 `"收工"兜底；/wrap`、"三个 index.ts"改"两个"
- 卸载：`pi remove one-memory`（含 `-l`）→ No matching package found；`pi list` 无包；`~/.pi` `~/.omp` 设置无 one-memory。本来就没装

关键决策：
- 判断"收工"轨已废弃。SKILL.md 分流表与 `references/write-memory.md` 都无此路径，`README.md` 的收工小节、`pi-extension/memory-rules.md` 的 `[SESSION_WRAP]`、`index.ts` 的 `/wrap` 是删漏残留 → 方向是删不是补。
- 审查中我判错三处，用户纠正：① 收工轨不该补回；② `write-memory.md` "整行等值比对"与 `pre-commit` "最近 5 条"不矛盾——等号是硬要求，5 条只是并发冗余；③ 技能内相对路径 `scripts/fts.py` 不是 bug，技能装固定位置，`install.sh:159` 还会把 fts.py 拷进 one-hippocampus/.git/hooks/。
- 用户认可的 bug：常驻读取门禁（"进入项目先读 rules.md"）写在 `references/rules-memory.md:114`，而该文件只在 write-rules 分支加载；正确位置是 `description` 或宪法文件 `one-agents.md`（那儿已有）。

涉及产物：`one-memory/`（无 pi 层）、`CODE_WIKI.md`。

---

## 规则轨范围分流 + 批评触发 + 宪法引路去重 + description 入口词

背景诉求：补完整局规则轨（海马仓 rules.md 骨架只有空 Entries）；用户复查指出两处问题要修。

改动：
- `one-agents.md`：第 7 行删「用户行为指令」半句（与第 8 行分流打架）；补第 8 行全局规则指针（教导/批评按 one-memory 分流）
- `one-memory/SKILL.md`：description 改为 138 字——补入口词「查记忆/查偏好/查流水/看画像/查历史任务、写流水/记偏好/记规则」，批评/指责触发词保留，去掉路径细则
- `one-memory/references/rules-memory.md`：加范围分流表（项目 rules.md vs 海马 rules.md）；触发条件 5 改「用户批评或指责 AI」（你怎么又…/我说过…/不对）；删「首次回复前必读」
- `one-memory/README.md` / `read-memory.md` / `write-memory.md`：同步分流、全局规则读写路径

同步推走（海马仓 cc7409d）：`rules.md` 边界文件头 + 7 条迁入 Entries；INDEX/readme 四态；preferences 7 条行为规则迁出。

关键决策：
- 行为指令只走 one-agents 第 8 行分流，第 7 行只管「进项目读 + Agent 写项目知识」——避免同一触发两个去向。
- description 必须带字面入口词（查记忆/查偏好…），否则路由打不中；分流细则不塞 description，进 references。

涉及产物：`one-agents.md`、`one-memory/`（6 文件）、海马仓 `rules.md`/`preferences.md`/`INDEX.md`/`readme.md`。

---

## 赛马技能 description 改手动触发

背景诉求：用户发现两个赛马技能的 description 在写作文。

改动：
- `one-orca-race/SKILL.md`：description 139→128 字，原句全是机制（支持哪些品牌、自动建 worktree）且无触发词，补「赛马/多 Agent 同题竞跑/并行跑几个 Agent 比谁写得好/orace」
- `one-paseo-race/SKILL.md`：description 287→134 字，砍掉「和 Orca 版本的区别…换取远程 daemon、--output-schema 结构化判卷」这段原理与卖点
- 用户随即指出：两个都不需要自动触发，全是手动触发 → 两个都加 `disable-model-invocation: true`，description 去掉触发词列表，只留一行人读的「是什么」

关键决策：手动触发的技能走 user-invoked，不付常驻上下文负担，写触发词没意义；原理/卖点/能力清单归正文。

涉及产物：`one-orca-race/SKILL.md`、`one-paseo-race/SKILL.md`。

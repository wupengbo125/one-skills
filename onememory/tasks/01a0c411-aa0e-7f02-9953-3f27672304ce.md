# 01a0c411-aa0e-7f02-9953-3f27672304ce

## 删除 one-memory 的 pi-extension 扩展层

背景诉求：用户用 one-writing-skill 让我审 one-memory，指出多个问题后决定不再要 Pi/OMP 扩展层，直接删掉。

改动：
- 删 `one-memory/pi-extension/`（index.ts、memory-rules.md）
- 删 `one-memory/package.json`（纯 pi 清单，指向已删目录）
- `one-memory/README.md`：清目录树 pi 条目、删"模式 B：Pi & OMP 原生扩展"整段、删"收工记忆"小节、"三轨"改"两轨"
- `CODE_WIKI.md`：删目录树 pi 条目、删 one-memory 扩展接口行、路由表去 `"收工"兜底；/wrap`、"三个 index.ts"改"两个"
- 卸载：`pi remove one-memory`（含 `-l`）→ No matching package found；`pi list` 无包；`~/.pi` `~/.omp` 设置无 one-memory。本来就没装

关键决策：
- 判断"收工"轨已废弃。SKILL.md 分流表与 `references/write-memory.md` 都无此路径，`README.md` 的收工小节、`pi-extension/memory-rules.md` 的 `[SESSION_WRAP]`、`index.ts` 的 `/wrap` 是删漏残留 → 方向是删不是补。
- 审查中我判错三处，用户纠正：① 收工轨不该补回；② `write-memory.md` "整行等值比对"与 `pre-commit` "最近 5 条"不矛盾——等号是硬要求，5 条只是并发冗余；③ 技能内相对路径 `scripts/fts.py` 不是 bug，技能装固定位置，`install.sh:159` 还会把 fts.py 拷进 one-hippocampus/.git/hooks/。
- 用户认可的 bug：常驻读取门禁（"进入项目先读 rules.md"）写在 `references/rules-memory.md:114`，而该文件只在 write-rules 分支加载；正确位置是 `description` 或宪法文件 `one-agents.md`（那儿已有）。

涉及产物：`one-memory/`（无 pi 层）、`CODE_WIKI.md`。

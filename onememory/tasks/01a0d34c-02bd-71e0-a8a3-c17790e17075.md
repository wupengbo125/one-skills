# 01a0d34c-02bd-71e0-a8a3-c17790e17075

## 诉求
用户要求对 Paseo 的待办插件 `paseo-plugins/one-todo` 做架构优化，硬约束只有一条：**用户可见功能不变**（界面/按钮/流程/结果/RPC/数据格式照旧），内部结构随便改。用 `improve-codebase-architecture` 扫描后自行拍板实施，不拿技术决策烦用户。

## 排查/结论
- 热点就是 one-todo：server/todo.ts 837 行、client/todo.tsx 1905 行，职责全堆一起。
- 用户明确纠正过判断标准：**改内部数据结构 ≠ 改功能**；只有把"微信变成王者荣耀"才算改功能。据此把原先被我排除的 schema 类改动也纳入。
- **分支起点事故**：worktree 分支 `one-todo-架构优化-a1` 从 `5257c0e` 拉出，落后 main 的 `e9a1fdf`（开跑前校验 workspace 失效并按项目重建）。重构代码因此不含该修复，被另一 Agent 评审判为"严重回归"。根因是起点旧，不是删代码；提交本身在 main 未丢。已把分支 base 对齐到 `e9a1fdf` 并把修复逐条搬进新结构。

## 改动
服务端：
- 拆出 `server/executor.ts`（handleStartTodo + 结算 + workspace 失效校验/项目回退/归档关联 stashWorkspaceProject）、`server/github.ts`（gh/git）、`server/placement.ts`（落点归一，保留 workspace 携带项目元数据）、`server/preferences.ts`、`server/migrations.ts`（纯迁移）。
- `server/store.ts` 只留待办仓储；`server/todo.ts` 836→约 260 行。
- `shared/todo.ts`：Todo schema 去 `provider`/`model`/`agentId`（可从 agents/agentIds 派生），老数据由 migrations 迁移；`primaryAgent` 提到 shared。
客户端：
- 拆出 `client/styles.ts`（createStyles）、`client/model.ts`（类型+小工具）、`client/primitives.tsx`（StableInput/PulsingPurpleDot）；`client/todo.tsx` 1905→1353 行。
- 补齐 e9a1fdf 的客户端改动（openDetail 回填 workspace、保存带 workspaceId/Name、输入框 key 去掉 run.id）。

## 验证
- `tsc --noEmit` 0 错误；新文件 prettier 通过。
- 迁移逻辑用真实 `todos.json`（18 条）演练：无遗留旧字段、运行中待办 agentIds/pendingAgentIds 保留。
- 落点规则与 `e9a1fdf` 12 组边界逐一等价（含"workspace+project"关键组合）。
- 已 rsync 到插件注册目录并 `paseo plugin reload`：running / Plugin ready，无报错。

## 遗留
- 前端 4 个 Modal（编辑、Agent 选择、技能选择、项目选择）与 15+ useState 仍在单组件内，未拆（风险集中在输入框/中文输入法，等用户实测后再继续）。
- 备份：数据 `~/.paseo/plugin-data/one-todo.backup-20260924-203026`；改动 tarball `~/.paseo/one-todo-refactor-20260924-210904.tgz`。

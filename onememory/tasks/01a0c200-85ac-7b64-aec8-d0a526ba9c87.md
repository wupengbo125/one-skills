# 会话 01a0c200-85ac-7b64-aec8-d0a526ba9c87

## 背景诉求
GitHub Issue wupengbo125/one-skills#2：给 one-orca-race 的 `-i` 参数加负数倒数支持。
`-i -1` 取最新 open issue（等价于不传 `-i`），`-2` 取倒数第二条，依此类推。

## 排查/决策
- `-i` 当前支持编号或 URL；未指定时取最新一条 open issue。
- 负数处理：在抓取逻辑前新增分支，正则 `^-[0-9]+$` 命中则用
  `gh issue list --state open --limit N --json number --jq .[N-1].number` 取倒数第 N 条；
  不足 N 条（jq 取空）清晰报错退出，不静默失败。

## 代码结论
- `one-orca-race/scripts/race`：新增负数解析块 + 更新 usage 说明文案。
- `one-orca-race/SKILL.md`：参数说明补充负数用法。
- 验证：`race -a omp -i -1 --dry-run` 正确解析为最新 issue #2；
  `-i -2` 因当前仅 1 条 open issue 报「可能不足 2 条」并退出（符合预期）。

## 产物
分支 `wupengbo125/918-codebuddy-orca-race-添加-数字` → 合并 main 即生效
（`.codebuddy/skills/one-orca-race` 为软链指向 main  checkout）。

# one-todo 全军出击点击改为提醒长按

## 诉求
将 paseo 插件里面的全军出击点击改为提醒长按，屏幕居中显示 2 秒轻量提示框（上下尺寸为原 3 倍、左右 1.5 倍），提醒请长按全军出击。

## 改动
- `paseo-plugins/one-todo/client/todo.tsx`：`HoldToLaunch` 短按触发 `onShortPress`，在弹窗居中层渲染轻量 HUD 提示框（上下 30px、左右 36px，展示「请长按全军出击」），并在 2 秒后自动隐藏。

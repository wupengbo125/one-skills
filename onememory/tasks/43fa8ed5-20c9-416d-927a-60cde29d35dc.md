# one-todo 全军出击点击改为提醒长按

## 诉求
将 paseo 插件里面的全军出击点击改为提醒长按，一个非常轻量的弹出框显示 2 秒，提醒请长按全军出击。

## 改动
- `paseo-plugins/one-todo/client/todo.tsx`：在 `HoldToLaunch` 组件中增加 `toast.show("请长按全军出击", { durationMs: 2000 })` 的 `onPress` 处理，短按点击未达 2 秒长按时弹出轻量提示框显示 2 秒。

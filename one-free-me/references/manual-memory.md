# 手工记忆指南

用户输入“收工”时触发。

## 执行步骤

1. **价值判定**：
   - 检查本次会话是否有未记录的硬核配置、踩坑或架构决策。无实质增量则直接回复已完成。
2. **沉淀情景长文**：
   - 写入 `memory/<YYYY-MM-DD_中文主题>.md`，包含背景、改动脉络、资产清单与避坑。
3. **记历史流水**：
   - 向 `history.md` 追加一行：`- YYYY-MM-DD HH:MM：<简述干了什么>`。
4. **提交 git**。
5. **极简反馈**：只答一句话结果。
---
name: one-awesome-design
description: 做 UI、网页或前端组件设计时使用。根据需求从 reference/ 目录读取对应的设计规范并遵循实现。
---

# Awesome Design

做 UI 或前端设计时，根据用户指定或需求意图，读取 `./reference/` 目录下对应的设计规范文件（如 `apple-design.md` 等），严格遵循其中的色彩、排版、组件及设计约束进行实现。无法确定时先列出 `reference/` 下的风格供用户选择。

---
name: one-grill
description: Use when user say "讨论需求" or "grill" or "prd".
---

# Grill With Docs PRD

Run a `/grilling` session

1. 必须先运行上述 skill，再进入后续流程。
2. **先研究，后提问**：如果需求是迁移、兼容或替代已有系统，先读取源系统代码、文档和现有实现，说明“源系统现在怎么做”以及迁移后用户能观察到的差异。源系统已有明确行为时，以保持该行为为默认方案，不把其内部实现反问给用户。
3. **Agent 承担技术判断**：可逆、内部、不会改变用户体验的实现细节由 Agent 根据源系统和仓库约束决定并记录理由。只询问必须由用户决定的目标、业务规则、风险接受度和可观察行为。
4. **问题使用用户语言**：先给已验证事实和推荐默认值，再问是否需要偏离默认值。选项描述结果和代价，不使用用户未理解的实现术语迫使其猜测。
5. 每个问题必须调用 `ask_question` 工具，禁止在聊天框文字提问。每次只问一个决策；所有问题都允许自由输入，不得把用户限制在预设选项中。用户回答“不知道”时，采用源系统行为；源系统没有对应行为时采用推荐方案。
6. 结束前用允许自由输入的弹窗展示确认 PRD，并确保每个决策都清晰标注继承了哪些源系统行为、改变了哪些用户可观察行为、哪些是 Agent 技术决策。获得用户明确确认后方可结束。
7. 禁止编写代码。
